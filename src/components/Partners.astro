---
interface Partner {
  name: string;
  area: string;
}

interface Props {
  partners: Partner[];
}

const { partners } = Astro.props as Props;
const basePath = import.meta.env.BASE_URL.replace(/\/$/, '');
const toPublicPath = (path: string) => `${basePath}${path}`;
---

<section id="partners" class="section-space bg-surface-50">
  <div class="site-container">
    <p class="section-eyebrow">Partners</p>
    <h2 class="section-title">パートナー</h2>
    <p class="section-summary">
      各分野の専門パートナーと連携し、建築設計・可視化・教育・構造の領域を補完しています。
    </p>

    <div class="mt-10 grid gap-6 lg:grid-cols-[1.1fr,0.9fr]">
      <div class="photo-frame">
        <img
          src={toPublicPath('/assets/partners-network.jpg')}
          alt="協働ネットワークのイメージ"
          class="h-full min-h-72 w-full object-cover"
          loading="lazy"
        />
      </div>

      <div class="grid gap-4">
        {partners.map((partner) => (
          <article class="soft-card p-5">
            <h3 class="text-2xl font-semibold text-surface-900">{partner.name}</h3>
            <p class="mt-2 text-sm text-surface-600">{partner.area}</p>
          </article>
        ))}
      </div>
    </div>
  </div>
</section>
